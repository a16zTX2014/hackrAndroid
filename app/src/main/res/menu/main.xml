<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.example.hackrandroid.app.MainActivity"
    >
    <item android:id="@+id/discover"
        android:title="Discover"
        android:icon="@drawable/discover"
        android:showAsAction="always"
        />
    <item android:id="@+id/matches"
        android:title="Matches"
        android:icon="@drawable/matches"
        android:showAsAction="always"/>
    <item android:id="@+id/profile"
        android:title="Profile"
        android:icon="@drawable/profile"
        android:showAsAction="always"/>
</menu>
